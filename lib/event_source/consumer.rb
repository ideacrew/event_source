# frozen_string_literal: true

module EventSource
  # A consumer is a type of application, connected to a server via a supported protocol,
  # that is consuming messages from channels. A consumer MAY be consuming from multiple
  # channels depending on the server, protocol, and the use-case pattern.

  class Consumer
  end
end
