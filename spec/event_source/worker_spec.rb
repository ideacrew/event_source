# frozen_string_literal: true

require 'spec_helper'

RSpec.describe EventSource::Worker do
  let(:config) { { num_threads: 2 } }
  let(:queue_proxy) {}
  let(:response) {}

  context '' do
    expect
  end
end
